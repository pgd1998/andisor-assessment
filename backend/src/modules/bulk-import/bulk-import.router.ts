import { Router } from 'express';

/**
 * Bulk-import routes.
 *
 * Placeholder introduced in the CRUD phase so the app mounts cleanly; the
 * asynchronous queue-backed implementation lands in the bulk-import phase.
 */
export const bulkImportRouter = Router();
